import { ArgTypes, Canvas, Meta, Source } from '@storybook/blocks';
import * as LoadingSpinner from './LoadingSpinner.stories';

<Meta of={LoadingSpinner} name="Overview" />

# NL - Loading Spinner

<Canvas of={LoadingSpinner.SpinnerTest} layout="fullscreen" withToolbar />

## Import

<Source dark code={`import { NlLoadingSpinner } from 'neal-react';`} />

<ArgTypes of={LoadingSpinner} />
