import { ArgTypes, Canvas, Meta, Source } from '@storybook/blocks';
import * as InputStories from './Input.stories';

<Meta of={InputStories} name="Overview" />

# NL - Form - Input component

The `NlInput` component is part of the Form components redesign on the library.
It goes wrapped under the [NlFromWrapper](?path=/docs/components-forms-form-wrapper--overview) that share the information between the other components like `NlLabel` and `NlErrorMessage`.

<Canvas of={InputStories.InputDefault} layout="fullscreen" withToolbar />

## Import

<Source dark code={`import { NlInput } from 'neal-react';`} />
### Props
<ArgTypes of={InputStories} />
